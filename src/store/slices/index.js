import { cartReducer, cartActions } from "./cart-slice";
import { paginationReducer, paginationActions } from "./pagination-slice";
import { productsReducer, productsActions } from "./products-slice";

export { cartReducer, cartActions, paginationReducer, paginationActions, productsReducer, productsActions };
